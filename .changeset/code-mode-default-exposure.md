---
"@caplets/core": minor
---

Make Code Mode the default Caplets exposure mode. Configs that omit `options.exposure` now expose `code_mode` by default; progressive wrapper tools remain available by setting `options.exposure` to `progressive` or `progressive_and_code_mode`.
