---
"@caplets/core": patch
---

Persist top-level `let`, `const`, class, enum, namespace, and TypeScript declarations across reused Code Mode sessions with REPL-style redeclaration and failed-cell behavior.
