---
import "../styles/starwind.css";
import "../styles/global.css";
import { themeColor } from "../data/landing";

interface Props {
  canonicalUrl?: string;
  description?: string;
  ogImage?: string;
  ogType?: "article" | "website";
  title?: string;
}

const siteUrl = "https://caplets.dev";
const {
  canonicalUrl,
  title = "Caplets, capabilities instead of giant tool walls",
  description = "Caplets gives coding agents focused capability modes instead of giant MCP tool walls.",
  ogImage = "/icon.png",
  ogType = "website",
} = Astro.props;
const canonicalHref = canonicalUrl ? new URL(canonicalUrl, siteUrl).href : undefined;
const ogImageUrl = new URL(ogImage, siteUrl).href;
---

<html lang="en" class="dark">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/icon.png" />
    <link rel="apple-touch-icon" href="/icon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    {canonicalHref && <link rel="canonical" href={canonicalHref} />}
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    {canonicalHref && <meta property="og:url" content={canonicalHref} />}
    <meta property="og:type" content={ogType} />
    <meta property="og:image" content={ogImageUrl} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="theme-color" content={themeColor} />
    <title>{title}</title>
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>
    <div class="copy-status" role="status" aria-live="polite" aria-atomic="true" data-copy-status></div>
    <slot name="background" />
    <slot name="header" />
    <main id="main">
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>
