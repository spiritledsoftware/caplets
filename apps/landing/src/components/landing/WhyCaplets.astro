<section class="mx-auto w-[min(1180px,calc(100vw_-_32px))] scroll-mt-20 py-20" id="why" aria-labelledby="why-caplets-title" data-reveal>
  <div class="grid gap-10 lg:grid-cols-[0.85fr_1.15fr] lg:items-start [&>*]:min-w-0">
    <div class="max-w-xl">
      <p class="text-sm font-medium text-muted-foreground">Why Caplets</p>
      <h2 id="why-caplets-title" class="mt-3 text-3xl leading-tight font-semibold tracking-[-0.02em] text-balance md:text-5xl">
        Stop carrying every handoff.
      </h2>
      <p class="mt-5 text-lg leading-8 text-pretty text-muted-foreground">
        Your agent changes the code. Caplets lets it continue through the systems you authorize.
      </p>
    </div>

    <dl class="divide-y divide-border border-y border-border">
      <div class="grid gap-2 py-5 sm:grid-cols-[9rem_minmax(0,1fr)] sm:gap-6">
        <dt class="font-semibold text-foreground">Keep moving</dt>
        <dd class="text-sm leading-6 text-muted-foreground">Carry work from issue to production without manual handoffs.</dd>
      </div>
      <div class="grid gap-2 py-5 sm:grid-cols-[9rem_minmax(0,1fr)] sm:gap-6">
        <dt class="font-semibold text-foreground">Keep control</dt>
        <dd class="text-sm leading-6 text-muted-foreground">Expose only the capabilities and credentials each host needs.</dd>
      </div>
      <div class="grid gap-2 py-5 sm:grid-cols-[9rem_minmax(0,1fr)] sm:gap-6">
        <dt class="font-semibold text-foreground">Keep focus</dt>
        <dd class="text-sm leading-6 text-muted-foreground">Open exact tools and schemas only when the task requires them.</dd>
      </div>
    </dl>
  </div>
</section>
