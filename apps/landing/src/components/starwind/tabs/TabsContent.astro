---
import type { HTMLAttributes } from "astro/types";

import { tabsContent } from "./variants";

interface Props extends Omit<HTMLAttributes<"div">, "role" | "tabindex" | "hidden"> {
  value: string;
  labelledBy?: string;
}

const { value, labelledBy, class: className, ...rest } = Astro.props;
---

<div
  class={tabsContent({ class: className })}
  data-tabs-content
  data-value={value}
  data-state="inactive"
  role="tabpanel"
  aria-labelledby={labelledBy}
  tabindex="0"
  hidden
  {...rest}
  data-slot="tabs-content"
>
  <slot />
</div>
