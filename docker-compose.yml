services:
  caplets:
    build:
      context: .
      dockerfile: Dockerfile
    image: caplets:local
    restart: unless-stopped
    environment:
      CAPLETS_SERVER_URL: ${CAPLETS_SERVER_URL:-http://127.0.0.1:5387}
      CAPLETS_REMOTE_SERVER_STATE_DIR: /data/state/caplets/remote-server
      CAPLETS_SENTRY_RELEASE: ${CAPLETS_SENTRY_RELEASE:-caplets-docker-local}
      CAPLETS_SENTRY_ENVIRONMENT: ${CAPLETS_SENTRY_ENVIRONMENT:-development}
      XDG_CONFIG_HOME: /data/config
      XDG_STATE_HOME: /data/state
    env_file:
      - path: .env
        required: false
    ports:
      - "${CAPLETS_BIND_ADDRESS:-127.0.0.1}:${CAPLETS_PORT:-5387}:5387"
    volumes:
      - caplets-data:/data
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          node -e "fetch('http://127.0.0.1:5387/v1/healthz').then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  caplets-data:
