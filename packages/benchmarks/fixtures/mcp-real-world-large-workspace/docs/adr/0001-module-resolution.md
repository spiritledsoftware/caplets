# ADR 0001: TypeScript Module Resolution

The fixture workspace uses NodeNext module resolution so imports match the package `type: module` setting.

Migration checks should compare local compiler options with current TypeScript guidance before recommending module-resolution changes.
